package com.example.pickme.models.enums;

/**
 * Represents the different status options for a waiting list entrant.
 */
public enum EntrantStatus {
    PENDING,
    APPROVED,
    REJECTED,
    REMOVED
}