package com.example.pickme.utils;

/**
 * Contains utility methods to initialize and interact with Firebase services
 * (Firestore, Firebase Storage, FirebaseAuth)
 * Responsibilities:
 * Initialize Firebase services
 * Provide methods to interact with Firebase services (e.g., upload files to Firebase Storage)
 * Other common Firebase operations (e.g., check authentication, fetch Firestore references)
 **/

public class FirebaseUtils {
}
